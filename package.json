{
  "name": "view-rendered",
  "version": "0.1.0",
  "description": "Appends a \"View Rendered\" set of links to PRs that include Markdown documents",
  "author": "Lee Dohm <lee@lee-dohm.com> (https://github.com/probot/view-rendered)",
  "license": "ISC",
  "repository": "github:probot/view-rendered",
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard"
  },
  "dependencies": {
    "probot": "^6.1.0",
    "probot-attachments": "git+https://github.com/probot/attachments.git"
  },
  "devDependencies": {
    "jest": "^21.2.1",
    "nodemon": "^1.17.2",
    "smee-client": "^1.0.1",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
